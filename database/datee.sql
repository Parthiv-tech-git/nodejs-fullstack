create database Itemdata ;
use Itemdata;
 create table dat(
 id int primary  key auto_increment,
 da  varchar(250));
 insert into dat(da) value('welcome my database'),
 ('ghjdsag');
 select *from dat ;
